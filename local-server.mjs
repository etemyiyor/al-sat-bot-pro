import http from 'http';import fs from 'fs';import path from 'path';import {pathToFileURL} from 'url';const root=process.cwd(),port=3000;function json(res,code,obj){res.writeHead(code,{'content-type':'application/json; charset=utf-8','cache-control':'no-store'});res.end(JSON.stringify(obj))}function body(req){return new Promise((ok,bad)=>{let s='';req.on('data',c=>s+=c);req.on('end',()=>{try{ok(s?JSON.parse(s):{})}catch(e){bad(e)}})})}async function api(req,res,u){let name=u.pathname.slice(5);if(!/^[a-z0-9-]+$/.test(name))return json(res,404,{error:'API bulunamadı'});let file=path.join(root,'api',name+'.js');if(!fs.existsSync(file))return json(res,404,{error:'API bulunamadı'});try{let mod=await import(pathToFileURL(file).href+'?t='+Date.now()),q=Object.fromEntries(u.searchParams.entries()),b=req.method==='POST'?await body(req):{};let r={method:req.method,query:q,body:b};let out={statusCode:200,headers:{}};let rr={status(c){out.statusCode=c;return rr},setHeader(k,v){out.headers[k]=v;return rr},json(o){res.writeHead(out.statusCode,{'content-type':'application/json; charset=utf-8','cache-control':'no-store',...out.headers});res.end(JSON.stringify(o));return rr},send(o){res.writeHead(out.statusCode,out.headers);res.end(typeof o==='string'?o:JSON.stringify(o));return rr}};await mod.default(r,rr)}catch(e){json(res,500,{error:e.message})}}function staticFile(req,res,u){let p=u.pathname==='/'?'index.html':u.pathname.slice(1);p=path.normalize(p).replace(/^\.\.(\/|\\|$)/,'');let f=path.join(root,p);if(!f.startsWith(root)||!fs.existsSync(f)||fs.statSync(f).isDirectory()){res.writeHead(404);return res.end('404')}let ext=path.extname(f),types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8'};res.writeHead(200,{'content-type':types[ext]||'application/octet-stream','cache-control':'no-store'});fs.createReadStream(f).pipe(res)}http.createServer(async(req,res)=>{let u=new URL(req.url,'http://127.0.0.1:'+port);if(u.pathname.startsWith('/api/'))return api(req,res,u);return staticFile(req,res,u)}).listen(port,'127.0.0.1',()=>console.log('\nAL-SAT BOT PRO çalışıyor:\nhttp://127.0.0.1:'+port+'\n\nDurdurmak için Ctrl+C\n'));