export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST gerekli'});const {symbol,side,quantity,price,stopLossPct=2,takeProfitPct=4,maxRiskPct=1,accountSize=10000}=req.body||{};const s=String(symbol||'').toUpperCase().trim(),sd=String(side||'').toUpperCase(),q=Number(quantity),p=Number(price),sl=Number(stopLossPct),tp=Number(takeProfitPct),risk=Number(maxRiskPct),acct=Number(accountSize);if(!s||!['BUY','SELL'].includes(sd)||!(q>0)||!(p>0))return res.status(400).json({error:'Sembol, yön, miktar ve fiyat gerekli'});const notional=q*p,maxAllowedRisk=acct*(risk/100),stopPrice=sd==='BUY'?p*(1-sl/100):p*(1+sl/100),takePrice=sd==='BUY'?p*(1+tp/100):p*(1-tp/100),estimatedRisk=Math.abs(p-stopPrice)*q;res.json({mode:'PREVIEW_ONLY',symbol:s,side:sd,quantity:q,price:p,notional,stopPrice,takePrice,estimatedRisk,maxAllowedRisk,riskCheck:estimatedRisk<=maxAllowedRisk?'PASS':'BLOCK',message:'Bu endpoint gerçek para emri göndermez.'})}